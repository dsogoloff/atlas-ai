# Question-bank answer-key audit — 2026-06-10

Generated by `pnpm convert:audit` (scripts/conversion/stage5-audit.ts). READ-ONLY: this audit changes no data — every SUSPECT below needs a human decision.

## Scope and method

* Bank parsed from the committed SQL: 11 hand-seeded rows (supabase/seed.sql, mirrored from 20260511000100_sam_l2_content_v1.sql), 79 generated rows (supabase/migrations/20260610151306_load_sam_questions.sql), 1 dev placeholder row. 11 generated rows share an external_id with hand-seeded rows and never land (on conflict do nothing) — effective bank = 80 questions.
* Answer-key evidence: Stage 2/3 artifacts under `..\atlas-stage4\scripts\conversion\output` (4 worksheet folder(s) found).
* Checks: structural shape per format; stem-stated arithmetic targets recomputed; stored answers cross-checked against the parsed answer key; misconception codes asserted against the seeded 21-code vocabulary; generated rows diffed against their stage3-tagged.json records. Where a check is not mechanically decidable the question is UNVERIFIABLE — nothing was guessed.
* §11 parity (generated INSERT byte-identical in migration and seed.sql): OK.

## Summary

| group | checked | clean | suspect | unverifiable |
|---|---:|---:|---:|---:|
| dev fixture (seed.sql only) | 1 | 0 | 0 | 1 |
| hand-seeded (Level 2 Placement Worksheet.pdf) | 11 | 11 | 0 | 0 |
| Level 1 Placement Worksheet.pdf | 12 | 5 | 7 | 0 |
| Level 2 Placement Worksheet.pdf | 11 | 9 | 2 | 0 |
| Level 3 Placement Worksheet.pdf | 20 | 15 | 5 | 0 |
| Level 4 Placement Worksheet.pdf | 25 | 20 | 5 | 0 |
| **total** | **80** | **60** | **19** | **1** |

## Suspects (19)

### SAM-L1-Q04 — inactive — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "Who is shorter, Lin or George?"
* Stored: correct_answer="Lin"
* **answer-format**: correct_answer "Lin" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L1-Q05 — inactive — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "Group A Group B\nIn which group does [object] belong?\nAnswer: Group …"
* Stored: correct_answer="B"
* **answer-format**: correct_answer "B" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L1-Q12 — inactive — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "Which set has more? Set A: [image] Set B: [image] Answer: Set ___"
* Stored: correct_answer="A"
* **answer-format**: correct_answer "A" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L1-Q19 — inactive — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "How many cherries are there? Write the number. Is the number of cherr…"
* Stored: correct_answer="9; yes"
* **answer-format**: correct_answer "9; yes" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L1-Q20 — ACTIVE — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "Write greater than or smaller than. 5 is ___ 7."
* Stored: correct_answer="smaller than"
* **answer-format**: correct_answer "smaller than" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L1-Q25 — ACTIVE — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "Write a fact family with these numbers: 6, 8, 2"
* Stored: correct_answer="6 + 2 = 8, 2 + 6 = 8, 8 – 2 = 6, 8 – 6 = 2"
* **answer-format**: correct_answer "6 + 2 = 8, 2 + 6 = 8, 8 – 2 = 6, 8 – 6 = 2" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L1-Q28 — ACTIVE — NUMERIC_ENTRY — Level 1 Placement Worksheet.pdf

* Stem: "Write the numbers in order. Begin with the smallest. 17 20 10"
* Stored: correct_answer="10, 17, 20"
* **answer-format**: correct_answer "10, 17, 20" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L2-Q04 — ACTIVE — NUMERIC_ENTRY — Level 2 Placement Worksheet.pdf

* Stem: "Abel, Beth, Cary, Dave and Ethan are queueing at the bank. Abel is in…"
* Stored: correct_answer="Ethan"
* **answer-format**: correct_answer "Ethan" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L2-Q08 — ACTIVE — NUMERIC_ENTRY — Level 2 Placement Worksheet.pdf

* Stem: "Write 96 in words."
* Stored: correct_answer="Ninety-six"
* **answer-format**: correct_answer "Ninety-six" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L3-Q02 — ACTIVE — NUMERIC_ENTRY — Level 3 Placement Worksheet.pdf

* Stem: "Write 508 in words."
* Stored: correct_answer="Five hundred and eight"
* **answer-format**: correct_answer "Five hundred and eight" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L3-Q11 — ACTIVE — MULTIPLE_CHOICE — Level 3 Placement Worksheet.pdf

* Stem: "Which of the following is equal to 18?"
* Stored: correct_index=1 → "9 × 3"
* **stem-arithmetic**: keyed option "9 × 3" = 27 does NOT satisfy stem states target "equal to 18"; option "9 × 2" (index 0) does
* **key-agreement**: stored correct_index 1 ("9 × 3") contradicts parsed key "1" → plausible index(es): 0 ("9 × 2"; key "1" read as 1-based option number)

### SAM-L3-Q13 — inactive — NUMERIC_ENTRY — Level 3 Placement Worksheet.pdf

* Stem: "What fraction of the figure below is shaded?"
* Stored: correct_answer="4/9"
* **answer-format**: correct_answer "4/9" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L3-Q18 — ACTIVE — NUMERIC_ENTRY — Level 3 Placement Worksheet.pdf

* Stem: "Name a 3-dimensional shape that has 2 flat faces and a curved surface."
* Stored: correct_answer="cylinder"
* **answer-format**: correct_answer "cylinder" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L3-Q22 — ACTIVE — NUMERIC_ENTRY — Level 3 Placement Worksheet.pdf

* Stem: "What is the greatest 4-digit even number?"
* Stored: correct_answer="9998"
* **key-agreement**: stored "9998" does not match parsed key "3"

### SAM-L4-Q15 — ACTIVE — NUMERIC_ENTRY — Level 4 Placement Worksheet.pdf

* Stem: "Aaron and Billy took part in a marathon. When Aaron had run 5 km 250 …"
* Stored: correct_answer="1 km 750 m"
* **answer-format**: correct_answer "1 km 750 m" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored
* **key-agreement**: stored "1 km 750 m" does not match parsed key "5 km 250 m = 5250 m"

### SAM-L4-Q20 — inactive — NUMERIC_ENTRY — Level 4 Placement Worksheet.pdf

* Stem: "The bar graph below shows the scores of five basketball teams in a to…"
* Stored: correct_answer="a) 150 b) 900"
* **answer-format**: correct_answer "a) 150 b) 900" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L4-Q22 — ACTIVE — NUMERIC_ENTRY — Level 4 Placement Worksheet.pdf

* Stem: "Mr Davis took 6 h 50 min to complete a marathon. He crossed the finis…"
* Stored: correct_answer="9:25 am"
* **answer-format**: correct_answer "9:25 am" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L4-Q26 — ACTIVE — NUMERIC_ENTRY — Level 4 Placement Worksheet.pdf

* Stem: "Round 42 750 to the nearest hundred."
* Stored: correct_answer="42 800"
* **answer-format**: correct_answer "42 800" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

### SAM-L4-Q27 — ACTIVE — NUMERIC_ENTRY — Level 4 Placement Worksheet.pdf

* Stem: "Name one number that can divide both 54 and 72."
* Stored: correct_answer="Accept 1, 2, 3, 6, 9 or 18"
* **answer-format**: correct_answer "Accept 1, 2, 3, 6, 9 or 18" is not plain-numeric — the judge (correctness.ts) falls back to EXACT string match and the child answers on a decimal-keypad text input, so this is likely unanswerable as stored

## Unverifiable (1)

No failure found, but the stored answer could not be mechanically confirmed either (no computable stem target and no usable parsed-key entry). These need eyeball review at lower priority than the suspects.

| external_id | active | format | source | stored | why unverifiable |
|---|---|---|---|---|---|
| PLACEHOLDER-Q-IMG-GRID-001 | inactive | MULTIPLE_CHOICE | dev fixture (seed.sql only) | correct_index=0 → "Cell 1,1" | stem-arithmetic: stem states no mechanically computable target; key-agreement: no parsed answer-key entry available |

## Hand-seeded vs pipeline duplicates

11 pipeline-converted rows were shadowed by the earlier hand-transcribed rows (same external_id, on-conflict-do-nothing). Independent-transcription agreement:

| external_id | agreement | detail |
|---|---|---|
| SAM-L2-Q01 | AGREE | same options + correct_index=2 |
| SAM-L2-Q07 | AGREE | same options + correct_index=1 |
| SAM-L2-Q09 | AGREE | same options + correct_index=1 |
| SAM-L2-Q10 | AGREE | same correct_order ["9","68","81"] |
| SAM-L2-Q11 | AGREE | same answer "28" |
| SAM-L2-Q14 | AGREE | same options + correct_index=3 |
| SAM-L2-Q17 | AGREE | same answer "16" |
| SAM-L2-Q19 | AGREE | same answer "648" |
| SAM-L2-Q20 | AGREE | same answer "604" |
| SAM-L2-Q21 | AGREE | same correct_order ["716","708","652","629"] |
| SAM-L2-Q22 | AGREE | same answer "780" |

