// Placeholder. Real dashboard ports stitch/module-d/01-parent-dashboard-desktop.html
// (active state) and stitch/module-d/02-parent-dashboard-empty-desktop.html
// (empty state), with the mobile variant at stitch/module-d/03-parent-dashboard-mobile.html.

export default function ParentDashboardPage() {
  return (
    <main className="max-w-container-max mx-auto px-gutter py-stack-lg">
      <h1 className="font-display-child text-sam-navy">Parent dashboard</h1>
      <p className="text-sam-gray-mid mt-stack-sm">
        Cycle 0 placeholder. Implementation pending.
      </p>
    </main>
  );
}
