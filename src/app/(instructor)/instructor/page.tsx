// Placeholder. Real instructor portal ports stitch/module-d/06..12 (roster,
// individual report, cohort view, with empty/active and desktop/mobile variants).

export default function InstructorHomePage() {
  return (
    <main className="max-w-container-max mx-auto px-gutter py-stack-lg">
      <h1 className="font-display-child text-sam-navy">Instructor portal</h1>
      <p className="text-sam-gray-mid mt-stack-sm">
        Cycle 0 placeholder. Implementation pending.
      </p>
    </main>
  );
}
